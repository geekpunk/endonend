server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routing (react-router): any path that isn't a real file
    # falls back to index.html so /album/... resolves in the browser.
    location / {
        try_files $uri /index.html;
    }
}
